
// Creating an string and then an alert for the browser to send the message
const string1 = ("You have received this message because you have been chosen to open an important vault. Here is the secret combination:");
alert(string1);

// testing the variables for the combination 
const numOn = 7 + 3;
const numTw = numOne + 30;
const numThre = numTwo - 1;

// checking the result in the console
console.log(numOn, numTw, numThre)